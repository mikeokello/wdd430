# TODO - W01 My First Next.js Application

- [ ] Create required `portfolio/` project folder structure with `portfolio/app` and `portfolio/components`.
- [ ] Move components from `app/api/hello/components/*` into `portfolio/components/*`.
- [ ] Update imports in `portfolio/app/layout.tsx`, `portfolio/app/page.tsx`, `portfolio/app/about/page.tsx` to use `@/components/*`.
- [ ] Ensure `portfolio/components/ProjectCard.tsx` matches rubric (typed props + conditional link + Tailwind + no `use client`).
- [ ] Ensure `portfolio/components/SkillCard.tsx` matches rubric (typed props interface + Tailwind + Server Component) and is rendered in `portfolio/app/about/page.tsx`.
- [ ] Update `README.md` with:
  - [ ] Vercel deployment URL placeholder
  - [ ] GitHub repo URL placeholder
  - [ ] Custom component filename + 1-sentence description
  - [ ] Reflection Q1 (1–2 sentences)
  - [ ] Reflection Q2 (1–2 sentences)
- [ ] Run `npm run dev` from the final project folder and verify `/`, `/about`, `/api/hello`.
- [ ] (Optional) Run `npm run build`.
- [ ] (Optional) Deploy to Vercel and replace URL placeholders.

